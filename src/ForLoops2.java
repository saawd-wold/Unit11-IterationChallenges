package src;

public class ForLoops2 {

    public static void main(String[] args) {
        int limit = Integer.parseInt(args[0]);
        // complete the line below to fill in the for-loop. 
        // just add the counter declaration, size check and increment operation.
        // Hint: how can you calculate the smallest even number that is at least as large as limit?
        for (int x = ; ;) {
            System.out.println("" + x);
        }
    }

}